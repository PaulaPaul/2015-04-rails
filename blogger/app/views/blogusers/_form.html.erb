<%= form_for(@bloguser) do |f| %>
  <% if @bloguser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bloguser.errors.count, "error") %> prohibited this bloguser from being saved:</h2>

      <ul>
      <% @bloguser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
